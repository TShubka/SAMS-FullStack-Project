package com.group8.sams.entity.enums;

/**
 * Authority names. These strings are what Spring Security sees, so they keep the
 * mandatory ROLE_ prefix.
 */
public enum RoleName {
    ROLE_ADMIN,
    ROLE_TEACHER,
    ROLE_STUDENT,
    ROLE_USER
}
